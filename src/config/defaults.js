// default params like model, temperature, limits

// Default parameters for LLM calls
module.exports = {
  model: "gpt-4",
  temperature: 0.7,
  maxTokens: 500,
  topP: 1,
  frequencyPenalty: 0,
  presencePenalty: 0
};
